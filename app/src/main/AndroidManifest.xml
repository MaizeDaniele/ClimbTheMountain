<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.climbthemountain">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">  <!-- used to set action bar-->
        <activity android:name=".RegisterAccountData" />
        <activity android:name=".RegisterSessionData" />
        <activity android:name=".RegisterSessionExam" />
        <activity android:name=".RegisterSessionSummary" />
        <activity android:name=".Login"></activity>
    </application>

</manifest>